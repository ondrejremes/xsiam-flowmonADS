#### Integrations
##### Flowmon ADS
- Added **Perspective Name** configuration parameter to filter fetched events to a specific ADS perspective by name (e.g. *Security issues*).
- Added client-side perspective filtering as a safety net to ensure events from other perspectives are never ingested regardless of API behaviour.
